# Feed Repair Desk

Build only this repository. Use synthetic data and reserved example.invalid URLs. No private files, account identifiers, transcripts, credentials, customer data or internal paths may enter source, examples, screenshots or Git history. Use system fonts and bundled dependencies. No external runtime API, telemetry, remote images, login or paid service.

Own the implementation through tests, production build and a private pushed branch. Do not alter repository visibility, enable hosting, merge, publish releases, create schedules, subscribe to PR events, arm check-in triggers, or continue unattended. Stop after the implementation handoff. The release owner reviews and publishes separately.

Every visible control must work. Preserve exact source cells and row order; show proposed vs applied repairs, unresolved facts and explicit undo. Reject malformed/corrupt imports transactionally. Do not make up product values or silently sanitize them. Use an explicit formula-risk export policy. Privacy and accurate status text are product behavior.

Conventional commit messages. Use Tom Hogan <248087815+tjhoags@users.noreply.github.com> as Git identity. No AI attribution or co-author markers. Do not add personal emails. This is a public-safe author identity, not a customer record.
